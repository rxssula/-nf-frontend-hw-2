import React from "react";
import Image from "next/image";

const TaskItem = ({ id, text, completed, handleToggleTask }) => {
  // Component that shows each TaskItem
  return (

  );
};

export default TaskItem;
